{
  "name": "font-atlas-gui",
  "version": "1.0.0",
  "description": "a gui for font-atlas",
  "browser": "index.html",
  "dependencies": {
    "@regexp-extra/variation-selector": "^0.1.1",
    "@tsconfig/recommended": "^1.0.1",
    "@types/css-font-loading-module": "0.0.4",
    "@types/node": "^14.0.27",
    "@types/webfontloader": "^1.6.32",
    "css-font": "^1.2.0",
    "promise-file-reader": "^1.0.2",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "browserify": "^16.5.2",
    "copyfiles": "^2.3.0",
    "sass": "^1.26.10",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:sass": "npx --no-install sass scss/:build/css/",
    "build:typescript": "npx --no-install -p typescript tsc",
    "build:browserify": "npx --no-install browserify build/js/main.js -o build/js/bundle.js",
    "build": "npm run build:sass && npm run build:typescript && npm run build:browserify"
  },
  "author": "Lucide",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Lucide/font-atlas-gui.git"
  },
  "bugs": {
    "url": "https://github.com/Lucide/font-atlas-gui/issues"
  },
  "homepage": "https://github.com/Lucide/font-atlas-gui#readme"
}
